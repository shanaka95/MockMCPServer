version: 1
applications:
  - appRoot: frontend
    env:
      variables:
        NODE_VERSION: '22'
    phases:
      preBuild:
        commands:
          - echo "Installing dependencies..."
          - npm ci
      build:
        commands:
          - echo "Building React app with Vite..."
          - npm run build
    artifacts:
      baseDirectory: dist
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/* 